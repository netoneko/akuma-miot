#!/bin/sh
# Generated by deploy.py for {agent} — do not edit on the box, edit the
# generator instead (overlays/deploy/deploy.py). An sshd session on Akuma
# sets no environment at all, so this wrapper exports everything itself
# rather than trusting the caller — same reasoning as
# ../akuma/scripts/box/akuma-dev.env.
{env_lines}
exec /root/kot/bin/kot run
